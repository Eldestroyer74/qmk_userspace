# ChieftainDots Roadmap

This roadmap is a backlog, not an approval list. Items start as ideas and must
be discussed, researched, and approved before implementation.

Use **Current Focus** for the next discussion threads. Use **Backlog** as the
long-term memory of ideas, kept decisions, rolled-back experiments, and evidence.

## Lifecycle

- Idea: captured but not prioritized.
- Discussed: intent and ergonomics are understood.
- Researched: QMK compatibility and size risk are understood.
- Approved: ready to implement as the next single feature.
- Implemented: code exists locally.
- Compiled: firmware builds successfully.
- Flashed: firmware has been put on the keyboard.
- Kept: real use confirms the feature should stay.
- Rolled Back: the feature was removed after compile, flash, size, or usability
  concerns.

## Top Maintainability Priorities

These are cleanup tasks, not behavior changes. Each should be handled as its own
small slice with a compile afterward unless the change is documentation-only.

| Priority | Status | Feature | Principle Challenge |
| --- | --- | --- | --- |
| M1 | Kept | Replace corrupted inherited layer diagrams in `layout.h` with clean ASCII row labels without changing keycodes. | Supports readability. Future diagram edits must remain mechanical and should verify keycodes before and after. |
| M2 | Kept | Audit legacy layout aliases, converters, inactive recipes, archived helpers, disabled combo scaffolding, and inherited aliases. Current action: removed stale Filterpaper-inherited `keymaps/cradio.json` and `keymaps/technik.json`, plus their unused wrappers/converters, because they referenced removed `_FUNC` and no longer described current ChieftainDots. The old helper code should remain retrievable from git history or the intended preservation tag `filterpaper-layout-wrappers-20260515`. | Supports ChieftainDots as a Corne-first project while keeping future keyboard-port knowledge retrievable without leaving broken inherited recipes in active source. Future multi-keyboard support should start from the current layer model with a fresh recipe and wrapper when a real keyboard port, such as Draculad or Mountain Ergo, is ready. |
| M3 | Idea | Simplify text-stub handling in `eldestroyer74.c` if more snippets are added, likely by using a small helper instead of repeating similar switch cases. | Supports future text-snippet growth, but private values must remain ignored and must not be committed. |
| M4 | Idea | Review tap-dance data declarations for readability and possible `static const` use where QMK allows it. | Supports clearer intent and possibly tiny size wins, but must be compiled because QMK tap-dance structs may require mutable storage. |
| M5 | Idea | Add or refine RGB helper comments around physical LED mapping and command-layer masks after each RGB behavior change. | Supports maintainable split-RGB debugging; future RGB changes must check QMK matrix coordinates, handedness flashing, and physical wiring before code assumptions. |
| M6 | Kept | Audit helper code that may no longer be needed in the active Corne build, especially legacy `TH_*` clipboard helpers, stale combo scaffolding, text-stub switch repetition, and any user-owned code that consumes bytes without serving current ChieftainDots behavior. Current cleanup removes the compiled legacy `TH_*` clipboard/tap-hold path from `layout.h` and `process_record_user()` because the active Corne layout does not use those keycodes and combos are disabled. | Supports firmware-size headroom and readability; distinguish preprocessor-only clutter from code that is actually linked into the firmware before deleting anything. Compile succeeded at 28270/28672 bytes, leaving 402 bytes free, a 126-byte improvement from the previous 28396-byte build. Future combo resurrection should rebuild combos deliberately rather than relying on the old `TH_*` path. |
| M7 | Kept | Clean the roadmap itself: remove duplicate IDs, merge duplicate feature rows, separate active backlog from settled decisions, keep long history in the trial log, and keep `Compiled`/`Implemented` as temporary states only. Current pass fixed duplicate maintainability rows and keeps the `F/J` timing experiment as a live flashed trial rather than pretending it is already kept. | Supports roadmap trust and makes future planning easier. This is documentation-only but should be reviewed carefully so useful history is not lost. |
| M8 | Kept | Architecture cleanup pass: check that each behavior lives in the intended owner (`layout.h`, feature module, RGB, OLED, or userspace hook) and move code only when it improves readability without changing behavior. Current pass updated the architecture document to match the active Numbers/Symbols/RGB model and added a source ownership table. | Supports elegance and maintainability. Any future code movement must compile and should be byte-neutral or byte-positive. |
| M9 | Kept | Remove stale inherited per-keyboard build rules for Cradio and Technik O from active `rules.mk`. The old layer recipes were already removed; leaving board-specific compile hooks behind implied support that ChieftainDots no longer provides. | Supports source-of-truth clarity. Future keyboard support should add a fresh recipe, wrapper, compile target, and any board-specific build rules together as one deliberate port. |
| M10 | Idea | Source-of-truth cleanup: reduce duplicated layer knowledge across `layout.h`, RGB masks, OLED status, printable guides, and docs by deriving from existing keymap/layer concepts where practical. | Supports elegance and prevents drift. Avoid adding generators or tables unless they reduce more duplication than they create. |
| M11 | Idea | Refactor the local Python printable-guide image renderer into committed userspace tooling. The renderer should generate an image from `docs/chieftainDots-printable-layer-guide.md` or its structured source and should make its Font Awesome dependency explicit, using the Font Awesome install already available on this machine where appropriate. | Supports keeping the printable guide and rendered image in sync. Once added, the workflow should call this renderer after printable-guide updates, verify the generated image, and commit both the script and any intended generated asset. |
| M12 | Discussed | Evaluate reducing the active layer count after the three-anchor layout trial to recover firmware bytes. | The first implementation should keep existing layer ids as a reversible behavioral trial. If the layout works physically, discuss whether now-unused layers such as dedicated Text, Snap, or old refinement paths can be removed or collapsed safely. Measure byte savings one layer/removal at a time and avoid hiding behavior in custom code just to reduce the layer table. |

## Current Focus

Start here if this project has been idle for a while. This short table is the
authoritative resume point; the longer table below is parked detail and history
from earlier discussions.

| Focus | Status | Topic | Resume Notes |
| --- | --- | --- | --- |
| R1 | Compiled | Flash and physically test the current three-anchor trial. | Current compiled firmware is `23402/28672` bytes, leaving `5270` bytes free. It includes the three-anchor layer model, Navigation hierarchy, GUI double-tap-hold Snap, Spanish angle quotes, personal-email mnemonic, Spanish `Win+Space` language switching, and delayed/blocking left Alt. It has not yet been marked Flashed or Kept for this final combined slice. |
| R2 | Test | Run the physical acceptance checklist before starting new code. | Test ordinary typing; Space/Enter hold as Shift; Numbers/Symbols/Function anchors; `Numbers + GUI = Navigation`; `Symbols + GUI = MS Styles`; `Function + GUI = Media`; Navigation tap/hold/double-tap/double-tap-hold; GUI double-tap-hold Snap traversal; Spanish accents, angle quotes, and language switching; delayed Alt with `Alt+Tab`, `Alt+Left/Right`, `Alt+F4`, `Alt+Shift+Arrow`, naked `Alt+Shift`, and naked `Alt+Space`; mnemonic snippets including `G`. |
| R3 | Parked | Check hardware first if strange behavior returns. | Recent apparent Space/keyboard failure was likely cable-related. Check USB side, TRRS, and the aviator cable connection before treating firmware as the cause. For the aviator cable, unscrew the join, fully detach it, reattach it, and tighten/reseat it. |
| R4 | Superseded | Earlier two-family simplification threads are archived, not active. | The active trial is now the three-anchor model: `D/K = Numbers`, `S/L = Symbols`, `A/; = Function`; each anchor has one GUI child. Older `S/L` structure plus `A/;` tools requirements remain useful design history but should not guide the next implementation unless the three-anchor trial is rolled back. |
| R5 | Deferred | Keep future ideas out of the current trial until it is accepted or rolled back. | Deferred items: double-Space sentence action, low-priority Windows shortcut helpers, broader gesture ladders for MS Styles/Media/Function, active layer-count reduction for byte savings, OLED review, and printable-guide image renderer tooling. |
| R6 | Follow-up | If the current trial is kept, update architecture/principles and consider cleanup. | After physical acceptance, update architecture and principles to describe the three-anchor model, mark superseded roadmap rows clearly, stage only the accepted files, check private text snippets, commit, then discuss whether unused layers or old trial scaffolding can be removed for byte/readability wins. |

### Parked Detail

These rows are retained as working notes from recent discussions. Use the resume
snapshot above as the next-action list; sort or archive these rows after the
current trial is kept, revised, or rolled back.

| Focus | Status | Topic | Decision Notes |
| --- | --- | --- | --- |
| F1 | Flashed | Auto-caps-after-punctuation debug trial. Current debug firmware uses console logging with OLED disabled. It capitalizes the next alphabetic key only after a word, sentence punctuation (`.`, `?`, `!`), and a following space. Spanish opening punctuation (`¿`, `¡`) should arm capitalization immediately for the next letter. | Decide whether this graduates from a logged debug trial into normal firmware, remains a data-gathering build, or rolls back. Keep the behavior separate from logging before any keeper commit. |
| F2 | Discussed | Home-row Shift reliability. `F` and `J` remain home-row Shift, but missed capitals still happen during real typing. | Compare narrow fixes one at a time: collected evidence, punctuation/auto-caps support, a one-shot Shift fallback, bottom-corner Shift backup, or a small timing experiment. Avoid broad eager hold behavior unless evidence shows it is worth the accidental-capital risk. |
| F3 | Idea | Explore double-tap-and-hold on home-row `F`/`J` as an explicit Shift gesture. | This could preserve home-row Shift while giving a more deliberate "I really mean Shift" path. Risks: high-frequency letters, double letters, rolls, and extra timing complexity on the same keys already doing mod-tap work. Research QMK/community practice and prototype only as its own trial. |
| F4 | Discussed | Simplify into two balanced command families rather than one forced parent. `S/L` becomes the structure/movement family; `A/;` becomes the symbols/tools family. | Target shape: `S/L` hold = Numbers, `S/L + left GUI` = Navigation, `S/L + left Alt` = MS Styles. Navigation absorbs Extremes and Snap through tap/hold/double-tap-hold on the arrow keys. `A/;` hold = Symbols, `A/; + left GUI` = Media, `A/; + left Alt` = Function/System. Text moves to mnemonic double-tap-hold snippets to reduce accidental private-string output. Base Space and Enter are tap-hold Shift; Numbers trials left Space as tap Space / hold Symbols while Enter remains Shift. |
| F5 | Discussed | Double-Space sentence action. Two quick spaces after a word could output `. ` and prime auto-caps. | Useful but intrusive because it rewrites text with Backspace. Keep paused unless sentence flow remains a real pain after the auto-caps trial. |
| F6 | Implemented | Safer Tab/Escape/Close tap dance. Double-tap Tab still closes when used alone, but sends Tab instead when Alt or GUI is already active. | Keep isolated from the auto-caps/logger work so it can be retained even if the logger build rolls back. Needs physical confirmation before marking Kept. |
| F7 | Monitor | Caps Lock / Xiaomi mirror app quirk. The reverse-caps issue was seen mainly in the Xiaomi mirror phone app and did not recur in the latest ordinary typing session. | Do not add more firmware noise now. If it returns, gather a focused log and consider whether Caps Unlock should avoid firing on Shift. |
| F8 | Discussed | Reconsider deliberate Shift placement. Earlier candidate: bottom-corner hold = Shift and double-tap-hold = Spanish/ES-comp. Newer candidate: tap-hold Space and Enter for Shift, preserving simpler lower-corner Spanish access for capital Spanish compose. | Supports keeping home-row `F`/`J` Shift while adding a more deliberate Shift path. The bottom-corner idea is less attractive if it makes uppercase Spanish compose harder. Space/Enter Shift needs timing research because thumb keys are frequent and slower; false Shift on Space would be more disruptive than false Shift on Enter. |
| F9 | Idea | Explore double-tap Spanish/ES-comp as a Windows input-language switch. Microsoft documents `Windows key + Spacebar` as the default Windows 10/11 shortcut for switching input language and keyboard layout; older Language Bar guidance also mentions `Alt+Shift`. | Prefer `GUI+Space` for a ChieftainDots shortcut because it matches current Windows guidance and avoids the earlier `Alt+Space` host-shortcut problem. Treat this as separate from holding Spanish for accented characters. |
| F10 | Idea | Explore replacing the Text layer with mnemonic Base/Colemak double-tap-hold snippets: `H = Home`, `W = Work`, `P = Phone`, `M = Meeting`, `E = Email`, `N = Name`. | Supports lower chord count and first-letter memory. Plain double tap was too easy to trigger while typing, so the trial requires holding the second tap before any private string is sent. Treat as an isolated trial before removing `_TEXT`. |
| F11 | Discussed | Keep Media in the A-family tools side instead of Menu hold. Requirement: `A/; + left GUI` opens a compact arrow-shaped Media layer. Media actions: Up = Volume Up, Down = Volume Down, Left = Mute, Right = Play/Pause. | Resolves the same-hand Menu-hold tension and keeps Menu as a plain context-menu key. Previous/Next track stay removed. Verify whether Mute should be the generic media mute key or a Teams-specific shortcut before implementation. |
| F12 | Flashed | Simplified layer model trial. Target shape: Base Space and Enter become tap-hold Shift keys; `S/L` owns structure/movement; `A/;` owns symbols/tools; Text becomes mnemonic double-tap-hold snippets; Spanish stays on the lower corners. | Current physical trial works after reseating a suspect aviator USB cable. If Space, split disconnects, or apparent keyboard failure returns, check the USB cable, connector seating, USB side, and TRRS connection before treating firmware as the primary suspect. For the aviator cable specifically, unscrew the join, fully detach it, reattach it, and tighten/reseat it. Acceptance depends on ordinary typing reliability, especially false Space holds, Enter behavior, capital Spanish compose, mnemonic false triggers, and whether the two-family model feels easier than the previous A-family/S-family split. |
| F13 | Implemented | Add console evidence for the simplified layer model trial. Current short trial turns console back on for thumb-Shift evidence while disabling Space-gap/Space-probe logging. | Required log shape: thumb Shift source, key, tap/hold outcome, timing, active mods, and active layer. Keep logs sparse and trial-specific. Do not log private snippets or full text beyond the existing safe short-word typing context. Space-specific logging is disabled because the current goal is thumb-key Shift behavior, and the recent Space failure was likely cable-related. Needs compile and flash before this becomes the active physical trial build. |
| F14 | Discussed | Add an MS Styles layer for PowerPoint paragraph/bullet style traversal. Requirement: `S/L + left Alt` exposes the `Alt+Shift+Arrow` family: Left = promote paragraph/style level, Right = demote paragraph/style level, Up = move paragraph up, Down = move paragraph down. | Supports the `S/L` structure/movement family and gives the left-Alt chord a real daily-work purpose. Confirm in PowerPoint after implementation because Office shortcut behavior can vary by focus context. |
| F15 | Implemented | Add Spanish angle quotation marks `«` and `»`. Current trial: `Spanish + Caps = «` and `Spanish + quote = »`, implemented as separate keys rather than a paired wrapper. | RAE/ASALE guidance prefers angular/Latin/Spanish quotes in formal Spanish, while English quotes remain common in many everyday and American Spanish contexts. This is a quality-of-Spanish-writing feature rather than a strict regional requirement. Avoid making ordinary apostrophe/quote typing worse. Spanish layer RGB should light the usable Spanish characters plus both Spanish hold anchors. Needs compile, flash, and physical app test. |
| F16 | Implemented | Add a personal-email mnemonic to the text-shortcut model. Current trial: `G = personal email`, while `E` remains the generic/work email mnemonic. | `G = Gmail` is memorable and avoids overloading `E`. The committed code uses `TEXT_STUB_PERSONAL_EMAIL`; the real address must live only in ignored `features/text_stubs_private.h`. Needs compile and physical test with the mnemonic double-tap-hold gesture. |
| F17 | Compiled | Explore a more balanced three-anchor / three-GUI-chord layer model. Compiled trial shape: Numbers, Symbols, and Function are peer anchor layers with one GUI-refined child layer each. Chord mapping: `Numbers + GUI = Navigation`, `Symbols + GUI = MS Styles`, and `Function + GUI = Media`. Current compiled Navigation is editing-first: tap = arrows, hold = extremes, double-tap-release = selection movement, and double-tap-hold = extreme selection. Snap moved out of Navigation into the GUI-owned Snap gesture captured in F19. MS Styles keeps `Alt+Shift+Arrow` in the arrow shape; Media keeps Up = Volume Up, Down = Volume Down, Left = Mute, Right = Play/Pause. The home-row command anchors shift one key inward, freeing the pinky-side home-row positions for Function anchors; Caps and quote remain plain tap keys. Function mimics the existing Numbers layer concept: the top-row number positions `1-5` become `F1-F5` on the left and `6-0` become `F6-F10` on the right, while the right-hand dial-pad block repeats `F4-F6` on the home row, `F1-F3` on the bottom row, `F11` where Numbers has `=`, and `F12` where Numbers has `/`. Function keeps established extras: Tab behavior, `DEL` in its current right Backspace-column location, and Colemak toggle in its current exact right-side position. Thumbs are explicit across the three anchor layers and child layers where useful for RGB; Numbers uses both thumb holds for Symbols access, while Symbols and Function keep thumb holds as Shift. Left Alt is available as `KC_LALT` on command layers unless a layer deliberately owns that position. Numbers and Symbols reuse visible `SLASH_PIPE` for tap `/`, hold `\`, double-tap `|`. Home-row `F/J` Shift leaves the design, and the old F/J Flow Tap exception was removed. | Motivation: the current simplified model works, but Function felt structurally different from Numbers and Symbols, while home-row Shift still caused occasional `F/J` timing doubt. Compile succeeded at 22970/28672 bytes, leaving 5702 bytes free. Physical test requirements: compare layer-entry comfort, RGB discoverability, explicit thumb lighting on Numbers/Symbols/Function and child layers, Numbers thumb access to Symbols, Symbols/Function thumb access to Shift, Left Alt availability, access to Navigation/MS Styles/Media, loss of home-row Shift, slash/backslash/pipe access from both Numbers and Symbols, Function access to Tab behavior, `DEL`, and Colemak toggle, Space/Enter or thumb Shift reliability, cleanup of obsolete `F/J` Shift optimisations, Navigation double-tap selection-movement and double-tap-hold extreme-selection ergonomics, GUI double-tap-hold Snap traversal, and whether this actually feels simpler than the previous two-family trial. |
| F18 | Idea | Low-priority Windows shortcut helpers if the three-anchor layout makes frequent shortcuts feel awkward. Candidate shortcuts: screen snip (`Win+Shift+S`) and clipboard actions such as Cut/Copy/Paste. | Filterpaper had named OS shortcut helpers, but they were mostly macOS/GUI-era patterns and should not be restored wholesale. First physically test whether the new Ctrl-on-home-row plus Shift/GUI-on-thumbs layout is comfortable. Add helpers only for shortcuts that are frequent, awkward, and worth a visible key position; prefer one or two named keys over reviving broad legacy shortcut/helper systems. |
| F19 | Idea | Explore relationship-first gesture ladders on child layers after the three-anchor trial settles. Navigation is the model: tap arrows, hold extremes, double-tap-release selection movement, and double-tap-hold extreme selection. Navigation selection movement should be directional rather than one-size-fits-all: Left/Right double-tap-release sends `Ctrl+Shift+Left/Right` to select the previous/next word; Up/Down double-tap-release sends `Shift+Up/Down` to extend the selection vertically from the cursor to the new line position. Repeating the double-tap-release gesture should continue extending the selection in the same direction. Apply the same tap/hold/double-tap/double-tap-hold pattern to MS Styles, Media, or Function-related child layers only when the gestures are strongly related to that layer's task. | Snap no longer belongs automatically inside Navigation if Navigation becomes text-editing-first. Preferred Snap requirement: double-tap-and-hold any exposed GUI key to hold GUI as a real modifier and momentarily expose a Snap arrow surface in the same `I/J/K/L` compass as Navigation. While the GUI gesture is held, `I/J/K/L` should send plain arrows, allowing repeated Windows snap traversal such as hold Win and press Left/Right/Up/Down multiple times. "Any exposed GUI key" means Base/Colemak and any anchor or child layer that deliberately exposes `KC_LGUI` or equivalent GUI behavior. Ordinary GUI tap/hold behavior must be preserved; only double-tap-hold should enter Snap. Prefer reusing the existing `SNP` layer id, changing its active keys from one-shot `Win+Arrow` macros to plain arrows while GUI is held. Add RGB feedback for the active Snap surface if size allows, ideally lighting `I/J/K/L` plus the active GUI source. This keeps Snap as GUI-owned window management rather than a Navigation behavior. This supersedes one-shot `Win+Arrow` macros and the rejected idea of holding Base/Colemak `I/J/K/L`, which was too risky because those are ordinary typing keys. MS Styles should first explore real Word/PowerPoint paragraph, bullet, outline, and style shortcuts before adding gestures. Media should stay compact unless a gesture clearly belongs with volume, mute, or playback. This is a requirements thread, not implemented behavior. |
| F20 | Compiled | Add deliberate Windows input-language switching to the Spanish lower-corner keys. Requirement: holding either Spanish key continues to enter the Spanish layer exactly as today; double tapping either Spanish key sends `Win+Space` once to open/cycle the Windows input-language selector; double-tap-and-hold holds GUI/Win and enters a temporary language-selector traversal mode. While held, `K` and Space should both send Space to move through the selector; releasing the Spanish key releases GUI/Win and chooses the highlighted language. | Prefer `Win+Space` over `Alt+Shift` because it is the modern Windows language selector and avoids the accidental Alt/Shift trap. RGB requirement: while language-selector traversal mode is active, light the active Spanish hold key, `K`, and Space. Kept separate from host-shortcut safety work so rollback is just returning the lower corners from `SPANISH_*_DANCE` to plain `MO(ESP)` and removing the Spanish traversal helper/RGB branch. Compile succeeded at 23402/28672 bytes, leaving 5270 bytes free. Needs physical test. |
| F21 | Compiled | Add host-shortcut safety for left thumb Alt. Requirement: the left Alt thumb should still support normal intentional shortcuts, including Alt alone, `Alt+Tab`, `Alt+Left/Right`, `Alt+F4`, and `Alt+Shift+key`, but naked `Alt+Shift` must not switch Windows input language and naked `Alt+Space` must not open the host/window menu. | Implementation is isolated behind `DELAYED_LALT` and `process_delayed_alt()`: Alt is delayed until a non-space, non-shift key proves the shortcut is intentional. Rollback is deliberately simple: replace `DELAYED_LALT` with `KC_LALT` in the layer recipe and remove the delayed-Alt helper. Compile succeeded at 23402/28672 bytes, leaving 5270 bytes free. Needs physical test against common Windows/Office shortcuts. |

## Backlog

This table intentionally preserves old decisions and rejected experiments. Prefer
promoting only the next few active topics into Current Focus instead of keeping
every active thought inside this long table.

| Priority | Status | Feature | Principle Challenge |
| --- | --- | --- | --- |
| 1 | Flashed | Investigate hold-tap timing because accidental symbol/number layers occur while typing. Current live trial keeps Flow Tap and Chordal Hold, but disables hold-on-other-key-press for `F`/`J` Shift mod-taps so rolling `f` into another letter is less likely to become accidental Shift. | Challenges home-row layer access and hold timing; timing changes must be small, measurable, and easy to roll back after real typing time. |
| 2 | Rolled Back | Audit and rebuild combos around the current ChieftainDots Corne layout. | Useful audit, but trial feedback showed the rebuilt inherited placements were not ergonomic enough to keep. |
| 3 | Kept | Design explicit editing/navigation behavior so selecting text does not depend on home-row modifier press order. Current answer: Navigation and Extremes expose plain Ctrl and Shift on the left hand, while arrows/extremes live on the right-hand `I/J/K/L` shape. The dedicated Selection layer was removed because selection can be composed from those pieces. | Supports explicit command layers and avoids solving this by wrapping command layers with `HRM(...)`. Future complaints should become new ergonomic bugs rather than reopening the old Selection-layer design. |
| 4 | Discussed | Investigate why Alt+Enter deletes or copies the last word. Current hypothesis: this may have involved inherited Filterpaper clipboard/tap-hold helpers or stale combo behavior. The compiled `TH_*` clipboard path has now been removed; retest before doing more code work. | Bug investigation before feature work; do not resurrect old combo or tap-hold helpers without a deliberate new design. |
| 5 | Idea | Reconsider CAGS/home-row strategy so frequent layers are easier to reach and GUI/Alt/Fn move to conventional positions. | Challenges home-row modifiers and thumb principles. |
| 6 | Rolled Back | Explore a better way to access Function/mouse shortcuts from both hands because Function currently lives on one side. Function access was intentionally moved to `A/; + left Space`; Mouse was intentionally removed to make room for Text Snippets and RGB/OLED headroom. | Function reachability is resolved through the A-family System layer. Mouse should return only as a new explicit feature with measured byte cost. |
| 7 | Kept | Bring back Menu. Current answer: the old right-thumb Function key became the context-menu/Application key because Function keys moved to `A/; + left Space`. | Supports typo-fix workflow and avoids spending a corner launcher position on Menu. |
| 8 | Kept | Add or restore a contextual Menu key because typo fixes are harder without it. Requirement: replace the old right-thumb Function access key with the QMK context-menu/Application key, because Function keys are now accessible through the A-family System layer. | Supports editing workflow and reuses a thumb key whose old layer-access purpose is gone. |
| 9 | Kept | Reconsider the bottom-left and bottom-right Shift keys because they may be underused. Current answer: both Base and Colemak use those corners as launchers, lower-left Media Player and lower-right Calculator. | Challenges old Shift-corner finger memory, but this was intentional and physically accepted. |
| 10 | Idea | Review whether left-hand number entry is worth the new memory burden. | Challenges calculator-style number placement and memorability. |
| 11 | Kept | Add `/` to the calculator/number layer to avoid changing layers during calculator use. Current answer: `/` lives on the same physical slash key in Numbers to preserve normal keyboard memory. | Supports one layer, one concept and normal keyboard transfer. |
| 12 | Rolled Back | Add full stop `.` to the left-hand side of the number layer. Current answer: the dot/comma problem was solved through right-hand number-pad tap dances on `2` and `3`, not by putting `.` on the left side. | Original placement idea was superseded by the current number-pad punctuation ladder. |
| 13 | Kept | Add or restore modifiers on non-base layers. Current answer: Navigation and Extremes expose plain left-hand Ctrl and Shift so selection can compose with arrows/extremes. Symbols and Numbers stay more literal to avoid nested hold behavior. | Supports the command-layer literalness principle. |
| 14 | Idea | Investigate rollover behavior with symbols. | Challenges symbol-layer access and hold-tap timing. |
| 15 | Idea | Make non-base layers use Escape where Base uses Tab. | Supports predictable non-base escape behavior. |
| 16 | Idea | Add a combo for toggling Caps Lock. | Supports caps behavior but may interact with combos and typing flow. |
| 17 | Idea | Run a firmware size audit without behavior loss; compile before and after, keep current behavior, and record bytes saved. | Supports future feature capacity without weakening current ChieftainDots behavior. |
| 18 | Kept | Add Spanish accented character access. Current answer: hold either lower corner for the Spanish layer; accented vowels stay on their letter positions, `N` sends `ñ`, `;` sends `ü`, `/` sends `¿`, and `1` sends `¡`. Uppercase accented vowels work by holding Shift first, then Spanish, then the vowel. | Supports bilingual typing while keeping normal quote/slash behavior intact. The first compose-key prototype is preserved at tag `spanish-compose-key-prototype-20260519`; the kept layer version compiled at 28104/28672 bytes, leaving 568 bytes free. Do not reopen Caps-hold or quote-hold access without a new physical problem: Caps/quote would restore Media/Calc corners but add hold-tap timing risk, and Colemak/right-home `;` conflicts with `ES + ; = ü`. |
| 19 | Idea | Add DaVinci Resolve shortcuts. | May require a dedicated app/workflow layer. |
| 20 | Idea | Add a shortcut for "SUB for book a meeting". | Needs macro architecture review. |
| 21 | Idea | Add a shortcut for "sub password". | Needs privacy, naming, and macro-storage review. |
| 22 | Approved | Make right thumbs transparent on command layers unless a layer deliberately owns them. Current motivation: `NAV` should allow existing right-thumb `RALT` to combine with arrows for browser/app history navigation without adding a new Alt key to the arrow grid. | Supports a consistent left-hand-enter/right-hand-act model. Right thumbs are already transparent on Colemak, Numbers, and Symbols; `NAV`, `EXT`, `SNP`, `MED`, `TEXT`, `SYS`, and `SPAN` currently block them without a layer-specific reason. Physical tests should check `NAV + RALT + J/L` for Alt+Left/Right, and make sure transparent Enter/Menu do not cause accidental actions. |
| 23 | Kept | Make RGB indicator-first by disabling decorative default animation and using RGB only for layer states, persistent toggles, or warnings. Current answer: RGB is primarily layer/state feedback; modifier RGB was removed because it was visually ambiguous. | Challenges visual identity in favor of lower distraction and clearer state feedback. |
| 23b | Kept | Replace the A-family Mouse layer with a Text Snippets layer. Requirement: `A/; + left Alt` opens six string stubs: `I = phone`, `J = meeting link`, `K = email`, `L = name`, `Y = home address`, and `H = work address`. Private Work/Home values must live only in ignored local `features/text_stubs_private.h`; committed code must use placeholders/examples. | Physically tested: strings work. Supports useful text shortcuts and frees Mouse Keys firmware space. Private strings require the workflow private-data gate before staging or committing. |
| 24 | Kept | Refine Caps Lock RGB so red indicates Caps cleanly without relying on the rough whole-keyboard fallback. Current answer: whole-board flat red is the accepted Caps signal because it is clear, byte-cheap, and works reliably after startup initialization. | Supports RGB-as-indicator. Do not revisit pulsing/animated Caps unless there is new byte headroom and a clear reason. |
| 25 | Idea | Review OLED screen layer indicators to confirm they report the intended active ChieftainDots layer state. | Supports state visibility; OLED should report keyboard state rather than owning key behavior. |
| 26 | Kept | Design one coherent Tab/Escape/Close key behavior: tap for Tab, hold for Escape, double tap for Close. | Tested successfully and kept as the Base Tab behavior; duplicate of item 30 retained for history. |
| 27 | Kept | Explore two anchor families: `S` for Numbers/Editing and `A` for Symbols/Tools, with left thumb refinements. | S/L-held numbered commands tested successfully and kept as a checkpoint. Still incomplete without navigation, selection, and extremes. |
| 28 | Idea | Define spatial consistency rules across command layers. | Supports learnability by keeping plus/minus aligned with scroll up/down, Enter/confirm aligned with primary action, and directional actions aligned across navigation sub-layers. |
| 29 | Idea | Make the Backspace position become Delete on non-base command layers. | Supports layer-aware editing: Base corrects typing with Backspace, command layers edit forward with Delete. |
| 30 | Kept | Make the Base Tab key tap for Tab, hold for Escape, and double tap for Alt+F4 close. | Tested successfully and kept as a checkpoint; supports one physical key having one memorable command family. |
| 31 | Kept | Move or keep the Colemak toggle on the future Media/System layer. Current answer: Colemak toggle lives on the quote position in `A/; + left Space` Function/System and lights in Colemak purple. | Supports persistent-mode controls living with other system/media commands instead of mixed into the numbered-command layer. |
| 32 | Idea | Research each ChieftainDots feature against QMK native support, current QMK docs, community practice, and implementation best practices before building or revising it. | Supports maintainability and avoids custom code when QMK already has a tested feature or common pattern. |
| 33 | Kept | Add navigation, word selection, and extremes refinements from the S/L numbered-command layer using left thumb holds and the `I/J/K/L` directional shape. | Tested successfully and kept; challenges home-row purity in favor of spatial consistency across arrows, selection, and extremes. |
| 34 | Kept | Add A-family Media layer: hold `A` plus left GUI for media controls. Updated layout: `Y = Play/Pause`, `H = Mute`, `I = Vol+`, `K = Vol-`, `J = Previous`, `L = Next`, Backspace position = Delete, right Shift unused. | Physically tested: Media works, with Previous/Next still app-dependent. Supports A-family tools and reuses spatial direction grammar plus the Y/H positive/suppressing operator pair. |
| 35 | Rolled Back | Retire the A-family Mouse layer and `MOUSEKEY_ENABLE` to make room for text snippets and future RGB work. The old mouse design was `A/; + left Alt`, with wheel on `Y/H`, pointer movement on `I/J/K/L`, and buttons on right thumbs. | Mouse worked physically but was traded away for higher-value text snippets and firmware space. Git history remains the rollback path if mouse support is needed again. |
| 36 | Kept | Add A-family Apps/System layer: hold `A` plus left Space, then use the right-hand Function/System surface; quote toggles Colemak and Backspace position remains Delete. | Supports keeping persistent keyboard-mode toggles deliberate without duplicating clean Windows shortcuts or filling a sparse layer with unrelated app launches. |
| 37 | Rolled Back | Restore keyboard-only window snapping on the Navigation layer by double-tapping direction keys. Current answer: do not use double-tap arrows because it breaks too many expectations; use the dedicated `SNP` layer on `S/L + left Space` instead. | Rolled back in favor of explicit snap commands and command-layer literalness. |
| 38 | Kept | Add Mute to the Media layer at `H`, paired vertically under `Y = Play/Pause`; right Shift remains unused on this layer. | Physically tested with Play/Pause and Mute working. Supports A-family media completeness and the positive/suppressing Y/H operator grammar. |
| 39 | Kept | Use underused lower-corner Shift positions on both Base and Colemak for launchers only: lower-left launches Media Player and lower-right launches Calculator. Colemak toggle is not part of this requirement. Keycodes are `KC_MSEL` and `KC_CALC`. | Challenges the old Shift-corner finger-memory principle, but supports useful launch commands without filling the sparse System layer. |
| 40 | Kept | Remove the legacy mixed Function layer from the active Corne build recipe and retire its OLED/RGB layer indicators. | Supports one-layer-one-concept by replacing a mixed legacy layer with explicit Numbers/Commands, Media, Text, System, and Base Menu behavior. |
| 41 | Kept | Make RGB use one scope rule across layer types: global typing states fill the board, modifier states light modifier positions, and command layers light their usable command surface. | Supports RGB-as-indicator and avoids two competing visual grammars for anchor layers versus persistent typing modes. |
| 42 | Kept | Move Function keys from the S/L Numbers/Commands layer into the A-family System layer so Function access remains consistent with the anchor/refinement model. Layout mirrors the right-hand number pad: `Y/H/N = F12/F11/F10`, `U/I/O = F7/F8/F9`, `J/K/L = F4/F5/F6`, `M/,/. = F1/F2/F3`, and quote toggles Colemak. RGB distinguishes the momentary Function/System command surface from persistent Colemak mode. | Physically tested successfully; challenges the current numbered-command layer, where Function keys lived with numbers; supports clearer A-family tools/system ownership. |
| 43 | Kept | Redesign Symbols so symbol characters live primarily on the top row to reduce rolling-key accidents and accidental symbol typing. Current implementation uses `! @ # $ % ^` on the left top row and `& * ( ) _ Delete` on the right top row, with lower symbol rows transparent so Base/Colemak letters still pass through. | Physically tested successfully; challenges the current two-hand Symbols layer and may improve typing flow by reducing home-row symbol collisions. |
| 44 | Rolled Back | Restore Filterpaper-style Colemak RGB as a whole-board Candy Rain typing-mode effect. Candy Rain and parameterized animated variants were tested but looked wrong or cost too much complexity/space. Current answer: Colemak is whole-board flat dark purple. | Supports persistent-mode discoverability while keeping RGB simple and byte-conscious. |
| 45 | Researched | Research QMK settings and current community Corne/home-row-mod practice for reducing accidental typing from home-row modifiers and layer-taps. Current direction: keep QMK-native Flow Tap and Chordal Hold, avoid eager hold-on-other-key-press for typing keys, and judge changes by real failed sequences such as `f/`, `fi`, and rolling `f` into another letter. | Supports home-row reliability and should be evidence-led before changing timing again. |
| 69 | Flashed | Investigate accidental capital `O` when intending to type `f` then `o`. Current live trial disables hold-on-other-key-press for `F`/`J`, leaving Shift mod-taps to permissive hold, Flow Tap, and Chordal Hold. | Supports typing reliability. Needs ordinary workday typing before marking `Kept` or rolling back. |
| 70 | Discussed | Investigate intentional `F + /` failing to produce `?` during the current `F/J` timing trial. Current observation: `F + /` is producing `/`, which suggests the shift mod-tap is staying tap-like after eager hold-on-other-key-press was disabled. | This is the opposite side of the accidental-capital trial: ordinary `fo` should not become `fO`, but intentional Shift chords such as `F + /` for `?` and `F + I` for capital `I` must still be reliable. Compare possible fixes against prior experiments before changing timing again. |
| 72 | Flashed | Collect data on missed home-row Shift when intending capitals, especially sequences like intending `F` as Shift plus a letter but getting literal `f` plus that letter instead. Current report on 2026-05-20: home-row Shift is still desirable, and this may be partly a technique-learning issue rather than a firmware problem. Use `docs/chieftainDots-shift-trial-log.md` for examples and trial results. | Supports typing reliability. Track real examples before changing code: intended output, actual output, hand/key sequence, whether Shift was pressed before the letter, and whether the miss repeats after slowing down. Do not overcorrect by restoring broad eager hold-on-other-key-press, because that risks bringing back accidental capitals such as `fo -> fO`. The 2026-05-22 v2 log showed many legitimate `f...` words and no slash events, so broad correction is risky and slash-specific behavior is still unproven. Current debug firmware trials guarded auto-caps-after-punctuation with logging: after a word plus `. `, `? `, or `! `, the next alphabetic key receives weak Shift unless Shift is already held. Non-Shift modifiers, command/navigation keys, the Spanish layer, non-word symbols, and first exceptions `vs.`/`etc.` clear or block the state. Candidate future options remain one-shot Shift fallback, narrow punctuation-only eager behavior, small `F/J` timing experiments, or relocating Shift only if collected evidence shows the current model is not learnable. |
| 71 | Discussed | Investigate left Alt plus Space leaking to the host and opening ChatGPT. Current observation: pressing left Alt and Space can trigger the OS/app global `Alt+Space` shortcut before the intended ChieftainDots chord behavior is established. | Challenges thumb modifier reliability and the A/S-family refinement model. Diagnose whether this is a plain Base-layer Alt+Space shortcut, an A-family layer-resolution timing issue, or an OS-level shortcut that should be avoided by changing the chord path. Do not remove reliable Alt behavior without a deliberate replacement. |
| 73 | Discussed | Investigate double-Space as a sentence action: after a word, two quick Space taps would replace the first space with `. ` and prime auto-caps for the next letter. | Useful but more intrusive than auto-caps-after-punctuation because it rewrites text with Backspace. Community practice favors explicit "next sentence" macros or automatic Sentence Case; QMK Tap Dance is not a good fit for Space because Space is frequent and naturally repeated. If trialed, implement as a small guarded state machine, not Tap Dance: word required, no Ctrl/Alt/GUI modifiers, no Spanish/command-layer context, timeout around 250-300ms, log `double_space_sentence`, and test in ordinary editors before terminals/forms/password contexts. |
| 74 | Implemented | Refine Base Tab/Escape/Close so double-tap Tab does not close when Alt or GUI is already active. Current implementation: `TAB_ESC_CLOSE` still taps Tab, holds Escape, and double-taps `Alt+F4` when used alone; if Alt or GUI is already active, double-tap sends Tab instead. | Supports safer window/app traversal. Physical tests should check plain double-tap Tab still closes, while Alt+double-tap Tab and GUI+double-tap Tab continue traversal instead of closing. This is a small local tap-dance change and should be compiled before flash. |
| 75 | Discussed | Simplify command layers into two balanced families. `S/L` is structure and movement; `A/;` is symbols and tools. | Requirement shape: `S/L` hold = Numbers, `S/L + left GUI` = Navigation, `S/L + left Alt` = MS Styles; `A/;` hold = Symbols, `A/; + left GUI` = Media, `A/; + left Alt` = Function/System. Base Space and Enter are tap-hold Shift; Numbers can trial left Space as tap Space / hold Symbols while Enter remains Shift. Text snippets move to mnemonic double-tap-hold gestures if safe. Main risk is trading chord count for timing complexity inside Navigation. Child/detail requirements: item 79 covers Text snippets, item 80 covers Media, item 83 covers MS Styles. |
| 76 | Discussed | Explore adding Shift back to the bottom-left and bottom-right corner keys while keeping home-row `F`/`J` Shift. Consolidated gesture requirement: simple hold on either lower corner sends Shift; double-tap-and-hold on that same key enters Spanish/ES-comp. | Supports keeping home-row Shift for flow while adding a second, more deliberate Shift option for sentence starts, punctuation, and cases where `F`/`J` timing is unreliable. It also supports layer consolidation by giving one physical location a parent/child relationship: hold = Shift, double-tap-hold = Spanish. Main risk is making Spanish access more complex and timing-dependent; test whether double-tap-hold is discoverable and reliable enough before replacing the current simple Spanish hold. This should be treated as an ergonomic trial, not a rejection of home-row Shift. |
| 77 | Idea | Explore double-tap-and-hold on home-row `F`/`J` as an explicit Shift gesture while preserving ordinary home-row Shift behavior. | Supports an intentional capital-entry path without moving Shift off the home row. Main risk is putting more timing behavior on high-frequency typing keys, where double letters, rolls, and fast ordinary words may become harder to reason about. Research community practice and prototype separately from the bottom-corner Shift idea. |
| 78 | Idea | Make double-tap Spanish/ES-comp send the Windows input-language switch shortcut. Researched shortcut: Microsoft lists `Windows key + Spacebar` for switching input language and keyboard layout on Windows 10/11; older Language Bar documentation also lists `Alt+Shift`. | Supports bilingual typing when switching the host layout is more appropriate than sending Windows Alt-codes. Prefer `GUI+Space` because it is the current Windows shortcut and avoids `Alt+Space`, which previously leaked to the host and opened ChatGPT/window menus. Needs design decision: whether this belongs on both lower-corner Spanish keys, only one side, or a future ES-comp tap-dance key. |
| 79 | Idea | Replace the Text layer with mnemonic double-tap-hold snippets on Base/Colemak letters: `H = Home address`, `W = Work address`, `P = Phone`, `M = Meeting link`, `E = Email`, and `N = Name`. Current audit found no active alpha-letter double-tap-hold gestures; existing double-taps are Tab, `/`, Symbols brackets, and Numbers `2`/`3`. | Supports the lower-chord-count command model by removing `A/; + left Alt` Text as a separate layer. Plain double tap caused trouble during typing, so the safer requirement is double tap and hold on the second tap. Preserve private values in ignored `features/text_stubs_private.h`, and test accidental-trigger rate before deleting `_TEXT`. |
| 80 | Discussed | Keep Media under `A/; + left GUI` and make it arrow-shaped. Media has only four actions: Up = Volume Up, Down = Volume Down, Left = Mute, Right = Play/Pause. | Supports the A-family tools model and resolves the right-thumb/Menu same-hand access concern. This supersedes both the older Media reshuffle and the Menu-hold Media idea. Previous/Next track stay removed. Needs implementation in `layout.h`, RGB mask review for the arrow-only Media surface, compile, and physical test. |
| 81 | Discussed | Trial the simplified ChieftainDots layer model as one coordinated direction, implemented in reversible slices. Requirement summary: Space/Enter tap normally and hold for Shift; home-row `F/J` Shift remains available during the trial; `S/L` owns Numbers, Navigation, and MS Styles; Navigation keeps arrows and gains hold-for-Extremes plus double-tap-hold-for-Snap; `A/;` owns Symbols, Media, and Function/System; Text snippets move to mnemonic Base/Colemak double-tap-hold gestures; Spanish stays on lower corners for simple accent access. | Supports reducing chord clutter while keeping two memorable command families. Dismissed alternatives: one-anchor Numbers-only consolidation was too forced and made tools feel homeless; Menu-hold Media was tidy but same-hand awkward because the right thumb would open a right-hand arrow action layer; moving Spanish to bottom-corner double-tap-hold made uppercase Spanish compose harder; using Space/Enter for layer chords conflicts with the new need for thumb Shift inside Navigation/Extremes. Remaining risks: Space is very frequent and may false-trigger Shift; Enter-hold is probably safer; Navigation child gestures may trade fewer chords for timing complexity; mnemonic snippets can leak private values if triggered accidentally, so they now require holding the second tap; Function/System shape should be reviewed now that it is a tools layer rather than a Numbers child. Each slice needs compile, flash, physical test, and a rollback path before the next slice. If kept, update the architecture/principles docs to describe the two-family model and update the printable guide to match the physical layer map. |
| 82 | Discussed | Extend console logging for the simplified layer model trial. First implementation should log only Space/Enter tap-hold Shift outcomes and keep existing `F/J` Shift plus auto-caps evidence. Later, when Navigation child gestures and Menu-hold Media are implemented, add narrow logs for those actions. | Supports evidence-led layout simplification. Log fields should include source (`thumb` or `hrm`), key, resolved action, timing, mods, layer, and enough safe context to compare false holds against missed capitals. Avoid logging private snippets, password-like complex strings, or every ordinary alpha key. Keep each log line easy to parse for later stats. |
| 83 | Discussed | Add MS Styles as an `S/L + left Alt` structure layer for PowerPoint paragraph/bullet style traversal. Requirement: use the `Alt+Shift+Arrow` family in the arrow shape: Left = promote paragraph/style level, Right = demote paragraph/style level, Up = move paragraph up, Down = move paragraph down. | Supports a useful work-specific command layer that fits the movement/structure family. It also gives the left-Alt refinement a clearer role after Extremes moves inside Navigation. Needs implementation, RGB review, compile, and a PowerPoint physical test in a slide using master-defined bullet styles. |
| 46 | Rolled Back | Replace persistent flat-red blank-key mapping with a short whole-board red flash only when a blank command-layer key is pressed. This was explored, but current RGB derives useful-key lighting from the keymap and keeps blank keys quiet. | Red now remains reserved for Caps and Delete cues; do not add blank-key flash unless we deliberately rebuild that feedback path. |
| 47 | Kept | Add richer bracket behavior on the Symbols layer: tapping the top-row parenthesis keys sends round brackets, holding those keys sends square brackets, and double-tapping sends curly brackets. | Physically tested successfully; supports calculator/programming completeness, but adds timing behavior to command-layer keys and must be monitored for accidental bracket entry risk. |
| 48 | Kept | Add richer slash and quote behavior on Base and Colemak: tap `/` for slash, hold `/` for backslash, double-tap `/` for pipe; tap `'` for quote, hold `'` for grave, and double-tap `'` for tilde. | Physically tested successfully; supports symbol reachability without entering the Symbols layer, but adds tap-dance timing to normal typing keys and must be monitored for accidental output. |
| 49 | Kept | Redesign S/L editing refinements: remove the dedicated Selection layer, keep selection behavior available by using plain Shift and Ctrl with arrows/extremes, move Extremes to the left Alt thumb slot, and use the left Space thumb slot for `SNP`, a snap-only window layer. `SNP` uses the same `I/J/K/L` directional shape: `I/J/K/L = GUI+Up/Left/Down/Right`, with no Ctrl or Shift positions. Delete remains on the Backspace position, and RGB lights the usable command surface. | Physically tested: navigation, selection/extremes, and snap work. Selection ergonomics are low priority unless real use proves awkward. |
| 50 | Kept | Size audit priority 1: measure the firmware-size cost of `MOUSEKEY_ENABLE` and decide whether the A-family Mouse layer earns its bytes. Current answer: Mouse Keys were removed and the A-family Mouse layer was replaced by Text Snippets. | Mouse worked, but Text Snippets and firmware headroom are currently more valuable. Git history remains the rollback path if Mouse returns. |
| 55 | Idea | Add an accessible equals sign requirement. Current issue: `=` is hard to find during calculator/numeric work. Decide whether it belongs on Numbers, Symbols, or a tap/hold variant before implementation. | Supports calculator completeness and should be considered with operator placement rather than patched in randomly. |
| 51 | Idea | Size audit priority 2: replace animated OLED with a minimal ChieftainDots status OLED if space pressure continues. Keep useful layer/status feedback, but consider removing Bongocat-style animation and large art assets only after checking whether unused-helper cleanup can recover enough bytes. | Challenges visual personality in favor of firmware capacity; OLED should remain state feedback rather than decoration if bytes are tight. |
| 52 | Rolled Back | Size audit priority 3: research replacing QMK Tap Dance with a small custom tap/hold/double helper for only the ChieftainDots keys that need it. | Researched and rejected for now. QMK Tap Dance is behavior-sensitive and currently works; a custom helper would be a refactor, not clean unused-code removal. Reopen only if byte pressure becomes severe enough to justify retesting timing behavior. |
| 53 | Idea | Size audit priority 4: research a lighter RGB indicator implementation that preserves per-key layer feedback without carrying unnecessary RGB Matrix behavior. | Challenges current RGB Matrix dependency; higher risk because split RGB and handedness have already required careful debugging. |
| 54 | Idea | Size audit priority 5: treat layer removal and stale active-layer cleanup as architecture cleanup first and size reduction second. Removing Selection or unreachable Function concepts may help bytes slightly, but the main reason should be a cleaner layer model. | Supports readability and future feature work; likely smaller byte savings than disabling major QMK features. |
| 56 | Idea | Review and fix OLED/screen layer feedback so the displays accurately reflect the current ChieftainDots layer model, including Text Snippets, Snap, Extremes, Function/System, Colemak, and transient command-layer access. | Supports state discoverability. Must be checked against current OLED code, split state behavior, and the active `corne.json` layer order before implementation. |
| 57 | Discussed | Make the Numbers-layer `+` key dual-purpose: tap sends `+`, hold sends `=`. | Preserves fast access to frequent addition while restoring an otherwise stranded common character without adding Shift to Numbers or reshaping the number pad. |
| 58 | Idea | Create compact OLED artwork for the four home-row concepts: Symbols, Numbers, Control, and Shift. Use this as a minimal OLED change that keeps Filterpaper's Bongocat/status architecture intact and avoids importing a large generated icon set. | Supports discoverability for the keys that most need help while respecting the current firmware-size budget. Artwork should be purpose-built for the existing 6-byte OLED font glyph format, reuse or replace only a small number of glyphs, and compile-size impact must be measured before flash. |
| 59 | Idea | Explore a larger OLED redesign that spends available memory on chord discovery screens instead of Bongocat. The concept: when an anchor is held, the screens teach the available thumb refinements for Numbers/Symbols and related command families. Before trading away the cat, run the unused-helper audit to see whether code cleanup can fund the OLED bytes. | Challenges the visual-personality principle by trading the cat for more functional help. This should only proceed after measuring the bytes recovered by helper cleanup and/or removing Bongocat, then deciding that chord discoverability is more valuable than the animation. |
| 60 | Kept | Byte audit: disable or remove unused active-build features one at a time, starting with likely low-risk candidates such as Grave Escape support and RGB Matrix keypress tracking. Current kept change removes QMK `RGB_MATRIX_KEYPRESSES` because ChieftainDots RGB is state-driven by layers, Caps, Colemak, chord previews, and Delete cues rather than reactive keypress effects. | Supports "byte is king for unused code." Compile succeeded at 27792/28672 bytes, leaving 880 bytes free, a 372-byte improvement from the previous 28164-byte build. User flashed it and reported things are looking good. Do not remove behavior-visible features such as OLED, RGB, Caps Unlock, Tap Dance, Extra Keys, text snippets, or split recovery without an explicit design decision. |
| 68 | Kept | Try a Numbers-layer redesign that uses traditional number-row memory across both halves while retaining a right-hand numpad core and a symbol-shift refinement. Current kept shape: top row follows traditional number-row memory, right-hand lower rows keep compact number-pad entry, and the left-hand `F` position becomes a momentary Symbols key from Numbers. | Challenges the old calculator/operator grammar. Physical testing confirmed the current Numbers/Symbols trial works; remaining operator-memory concerns should become smaller follow-up requirements rather than reopening the whole layer. |
| 61 | Kept | Refine the Numbers/Symbols row trial so the edge positions carry regular keyboard symbols instead of Delete/blank behavior. Current implementation: Numbers top-left gets `` ` ``, Symbols top-left gets `~`, and quote is simplified back to a plain quote key with no hold/double-tap behavior. `=`/`+` should stay paired conceptually even if displaced from the top row. | Physically tested as part of the latest working commit. Challenges the rule that non-base command layers use Delete at the Backspace position; for Numbers/Symbols, character completeness matters more than Delete. |
| 62 | Kept | Refine Numbers punctuation memory: keep `/` on the physical slash key, remove `?` from Symbols, and make the right-hand number-pad `2`/`3` keys carry punctuation ladders: tap `2`/`3`, hold `,`/`.`, double-tap `<`/`>`. | Physically tested as part of the latest working commit. Supports normal keyboard transfer for slash and makes comma/dot reachable from the number pad without spending extra dedicated keys. |
| 63 | Kept | Refine RGB vocabulary for anchors and future choices. Base/Colemak idle home-row previews were abandoned after testing because they added noise. Pressed-modifier RGB was also removed because global modifier state made Ctrl and Shift ambiguous and lit opposite-hand modifiers that were not physically pressed. Active anchor layers light only their usable surfaces, and thumb keys preview the future refinement layer in that future layer's color while only half the chord is held. The `SYM` key inside Numbers is a faint Symbols-color hint. Symbols, Numbers, and their thumb-refined chord layers use more distinct colors. On thumb-refined command layers only, the Backspace/Delete position lights red when it is available as Delete. Caps Lock is flat whole-board red to keep the firmware under size limits. Physical trial notes: the leftmost LED columns on the left half still do not light, so the GUI thumb hint may not be visible even if the code targets it correctly; Snap and Function/System colors are too similar and need stronger separation. | Supports RGB-as-indicator and chord discoverability while keeping Base/Colemak visually quiet. Next RGB pass should avoid relying on physically unavailable left-side LEDs and choose a more distinct Snap/System color pair. Modifier RGB can return only with explicit physical-key tracking and distinct Ctrl/Shift colors. |
| 64 | Kept | Make the Colemak toggle key on the Function/System layer use the same RGB color as persistent Colemak mode. | Physically tested as part of the latest working commit. Supports future-state signaling: the key should show the mode it will toggle, not merely inherit the current System-layer color. |
| 65 | Kept | When a left-hand anchor plus thumb chord is fully pressed, turn off unused thumb hints and keep only the two pressed chord keys lit alongside the active command surface. | Physically tested as part of the latest working commit. Supports present-state clarity: half a chord teaches possible futures; a completed chord should show the active path instead of continuing to advertise alternatives. |
| 66 | Kept | Initialize RGB Matrix indicator mode at keyboard startup, not only on layer changes. | Physically tested as part of the latest working commit. Fixes the observed startup gap where Caps Lock red did not appear until after a chord/layer event. |
| 67 | Idea | Explore whether any reactive per-key RGB feedback is worth restoring after removing `RGB_MATRIX_KEYPRESSES`. Current removal saves about 372 bytes. Possible community-aligned uses: a very brief confirmation flash on successful text snippet output, a subtle keypress pulse only on command layers to confirm sparse-layer hits, or an invalid-key/error flash if we reintroduce blank-key feedback. Avoid decorative ripple/sparkle effects unless they clearly support the RGB visual language. | Challenges byte discipline and RGB-as-indicator. QMK reactive keypress effects require keypress tracking, and QMK's RGB feature machinery has a real firmware cost; restoring this should be treated as a feature that costs roughly 372 bytes plus any custom code, not as a default. |

## Trial Log

| Date | Feature | Result | Notes |
| --- | --- | --- | --- |
| 2026-05-07 | Apply home-row layer/mod wrapper to Numbers, Symbols, and Function layers in `keymaps/corne.json`. | Rolled Back. | Compiled with no size change, but real use showed Symbols and Numbers became confusing because command-layer keys were transformed into nested layer-tap/mod-tap behaviors. Filterpaper's current Corne example also wraps only typing layers (`BASE` and `COLE`) and keeps `SYMB`, `NUMB`, and `FUNC` plain. Future editing/navigation work should use explicit commands, explicit modifiers, or a dedicated editing layer instead. |
| 2026-05-07 | Make RGB indicator-first by default. | Flashed and tested. | Removed decorative Candy default/Colemak animations, added RGB Indicator V1 color vocabulary, kept RGB indicator hooks, and changed size from 28212/28672 to 27432/28672 bytes. Typing works and Caps Lock visibly turns RGB red, but Caps currently uses a rough whole-keyboard red fallback. |
| 2026-05-07 | Investigate Caps Lock RGB mapping. | Follow-up needed. | QMK's `CAP_FLAG` selection did not visibly light Caps on this board. A temporary last-key diagnostic showed confusing split behavior: left-side inner columns `R/T/F/G/V/B` and right-hand letter keys were useful observations, but custom diagnostic state is not synced across split halves. Future work should compare `keyboard.json` LED indexes, split handedness, and physical LED wiring before changing behavior. |
| 2026-05-07 | Audit current combo reachability. | Follow-up needed. | Active combos appear partly inherited from Filterpaper. `tog_fnc` and `tog_num` likely reference old thumb keys no longer present in the current Corne layout. Combos involving `F`, `J`, `K`, and `;` may not fire because the current `HRM(...)` wrapper changes those exact keycodes into mod-tap or layer-tap keycodes. `I+O` is likely reachable, and `M+,` may be reachable. Future combo work should verify generated keycodes before implementation. |
| 2026-05-07 | Rebuild combos around current Corne layout. | Compiled. | Updated active combos to use current exact keycodes: `HM_F+KC_G`, `KC_M+KC_COMM`, `HM_SCLN+KC_SLSH`, and existing HRM key aliases for right-hand navigation. Disabled stale `tog_fnc` and `tog_num` combos because they referenced old Filterpaper thumb keys. Firmware changed from 27456/28672 to 27430/28672 bytes. Needs flash and physical combo trial before marking Kept. |
| 2026-05-07 | Capture OLED layer indicator review. | Backlog added. | The keyboard has screens that appear to show layer state, but their behavior has not been checked against the current ChieftainDots layer model. Future review should inspect OLED rendering code, split state sync, and how Base/Colemak/Numbers/Symbols/Function are displayed. |
| 2026-05-08 | Disable combos after ergonomic trial feedback. | Compiled. | User feedback: HubSpot combo required leaving a home-row index position, Ctrl+Shift+Arrow combos overlap with an existing disliked navigation idea, and volume combos required awkward vertical finger postures. Combo feature is disabled while text snippets, numbers, navigation, and system/media chords are redesigned from ergonomics first. Firmware size improved to 24874/28672 bytes, with 3798 bytes free. |
| 2026-05-08 | Explore two anchor families. | Design note added. | Candidate model: `S` family covers Numbers, Arrows, Selection, and Extremes using left thumb refinements. `A` family covers Symbols, Function, Mouse, and Media/Volume using left thumb refinements. This has not been implemented. Prefer ordinary QMK layer stacking over combos or custom state machines unless plain layers prove inadequate. |
| 2026-05-08 | Implement S-held right-hand Numbers first slice. | Compiled. | `_NUMB` now blanks left-hand non-thumb keys and maps the right hand to `+ 7 8 9 * / - 4 5 6 / . / ( 1 2 3 0 )`. Existing `S` layer hold remains the access method; left thumbs and `L` hold are unchanged for this trial. Compile succeeded at 24874/28672 bytes, with 3798 bytes free. Needs flash and physical trial before marking Kept. |
| 2026-05-08 | Capture S-held numbered-command refinement. | Design note added. | Candidate next slice: put Function keys on the left side of the S-held layer using old number memory: `F12 F7 F8 F9`, `F11 F4 F5 F6`, `F10 F1 F2 F3`; keep right-hand numbers/operators; blank Tab/Caps/Shift positions; make the Backspace position Delete on this non-base layer. |
| 2026-05-08 | Implement S/L-held numbered commands and Tab/Escape/Close. | Kept. | `_NUMB` now uses `F12 F7 F8 F9`, `F11 F4 F5 F6`, `F10 F1 F2 F3` on the left and keeps right-hand numbers/operators with Delete on the Backspace position. Base Tab is now a tap dance: tap Tab, hold Escape, double tap Alt+F4. Compile succeeded at 25756/28672 bytes, with 2916 bytes free; physical testing confirmed functions and Tab behavior work. This is a working checkpoint, but the keyboard remains less useful than intended until navigation, selection, and extremes are added. |
| 2026-05-08 | Capture right-hand directional shape. | Design note added. | Directional command layers should use `I/J/K/L` as Up/Left/Down/Right. Selection and extremes should reuse the same physical shape. This deliberately prioritizes spatial consistency over strict home-row-only command placement. |
| 2026-05-08 | Implement navigation, selection, and extremes refinements. | Kept. | Added `_NAV`, `_SELE`, and `_EXTR` as plain QMK layers above `_NUMB`. Hold `S` or `L`, then use left GUI for arrows, left Alt for Ctrl+Shift selection, and left Space for Home/Page/End extremes. All three use the `I/J/K/L` directional shape. Navigation and extremes keep `D = Ctrl`, `F = Shift`, and Backspace position as Delete. Physical testing confirmed navigation, selection, extremes, Ctrl, and Shift behavior work. |
| 2026-05-08 | Capture A-family media layout. | Design note added. | Agreed `A + left GUI` media layout: `I = Volume Up`, `K = Volume Down`, `J = Previous`, `L = Next`, `, = Play/Pause`, with Backspace position as Delete. Symbols remain unchanged for now. |
| 2026-05-08 | Capture mouse wheel/operator consistency. | Design note added. | Mouse wheel placement should mirror calculator plus/minus: `Y = Wheel Up` corresponds to `Y = +`, and `H = Wheel Down` corresponds to `H = -`. Multiplication/division remain a vertical pair with `P = *` above `; = /`. |
| 2026-05-08 | Implement A-family media, mouse, apps/system, and base Menu. | Compiled. | Base old right-thumb Function position now sends `KC_APP`. Holding `A` plus left GUI enters Media, holding `A` plus left Alt enters Mouse, and holding `A` plus left Space enters Apps/System with Colemak toggle. Compile succeeded at 26546/28672 bytes, with 2126 bytes free. Needs flash and physical trial before marking Kept. |
| 2026-05-08 | Capture navigation/window-snapping conflict. | Follow-up needed. | User reported that keyboard-only window snapping no longer works because the left GUI thumb is now used to access arrows from the S/L family. Future navigation work must decide whether to add explicit GUI+Left/Right commands, move Navigation off GUI, or provide another clean OS-window-management route. |
| 2026-05-08 | Trial A-family Mouse layer. | Kept. | User reported that the mouse layer works as expected. Keep the implemented `A + left Alt` mouse layout while continuing to trial the rest of the A-family. |
| 2026-05-08 | Enable Flow Tap for hold-tap typing accidents. | Compiled. | Added `FLOW_TAP_TERM 150` as the first timing experiment for accidental `S`/`L` number-layer activation that produced characters such as `8` or `(` during normal typing. Compile succeeded, but firmware size rose to 27292/28672 bytes, with 1380 bytes free. Needs flash and typing trial before marking Kept. |
| 2026-05-08 | Capture Media/System candidates. | Design note added. | Add Mute near the volume controls, and consider Apps/System candidates such as Calculator launch. These are candidates only; placement and QMK/native support still need discussion and research. |
| 2026-05-08 | Refine Media Play/Mute placement. | Design note added. | Agreed Media should treat `Y = Play/Pause` like the positive/additive action and `H = Mute` like the subtractive/suppressing action. The previous `, = Play/Pause` placement should move when the next media implementation happens. |
| 2026-05-08 | Move Colemak toggle to right home row on System. | Compiled. | System is now intentionally sparse: hold `A` plus left Space, then press any right home-row key to toggle Colemak. Calculator and Media Player launchers stay in the backlog for a separate non-layer launcher design. Compile succeeded at 27292/28672 bytes, with 1380 bytes free. Needs flash and trial before marking Kept. |
| 2026-05-08 | Capture Base corner launcher requirement. | Requirement added. | User noted the left and right Shift positions are no longer used. Candidate requirement only: place Colemak toggle on the Base bottom-left corner, Calculator on the Base bottom-right corner, and Media Player on the Base layer rather than System. No code change yet. |
| 2026-05-08 | Remove legacy Function layer from active Corne recipe. | Implemented. | Removed `_FUNC` from `keymaps/corne.json`, renumbered active layer constants, and removed the stale Function color/OLED cases. The old `_FUNC` macro body remains in `layout.h` as unreachable inherited reference text until a separate mechanical deletion pass. Needs compile before marking Compiled. |
| 2026-05-08 | Implement RGB scope masks. | Compiled. | Replaced inherited keycode-introspection layer lighting with explicit command-surface masks for Numbers, Symbols, Navigation, Selection, Extremes, Media, Mouse, and System. Caps remains whole-board red, Colemak remains whole-board purple, and modifiers remain green. Compile succeeded at 27428/28672 bytes, with 1244 bytes free. Needs flash and physical RGB trial before marking Kept. |
| 2026-05-08 | Fix split RGB state sync. | Compiled. | Physical test showed `A + thumb` layers and `S + Alt` did not show command-layer RGB, while `S + GUI` and `S + Space` showed only the left-side modifier positions. This suggests the non-master half was not reliably receiving momentary layer state. Make split mods, host LED state, and layer state sync explicit in `config.h`, matching QMK's RGB Matrix guidance for split keyboards. Compile succeeded at 27428/28672 bytes, with 1244 bytes free. Needs flash and physical trial. |
| 2026-05-08 | Re-test RGB split sync patch. | Kept. | User reported the same symptoms after ordinary flashing: no RGB for `A + thumb` layers, lights for `S + GUI` and `S + Space`, and no lights for `S + Alt`. Flashing the canonical userspace JSON with `dfu-split-left` and `dfu-split-right` fixed the behavior, confirming the issue was stale or wrong `EE_HANDS` handedness rather than RGB masks alone. Future split RGB/OLED debugging must check handedness flashing first. |
| 2026-05-08 | Correct RGB visual-to-matrix mapping. | Kept. | Arrow and command-layer RGB masks used raw right-hand column numbers, but the Corne right half is visually reversed in matrix coordinates: `Y U I O P BSPC` maps to `5 4 3 2 1 0`. Replaced raw right-hand columns with named visual-position helpers for direction, media, mouse, symbols, delete, and thumb indicators. Compile succeeded at 27428/28672 bytes, with 1244 bytes free. Physical trial confirmed the RGB lights now land in the intended positions. |
| 2026-05-08 | Implement Media Y/H refinement. | Implemented. | Updated `A + left GUI` media so `Y = Play/Pause`, `H = Mute`, `I/K = Volume Up/Down`, `J/L = Previous/Next`, and comma is blank. RGB mask now lights `Y/H/I/J/K/L` plus Delete for the Media layer. Needs compile and physical trial. |
| 2026-05-08 | Implement Base lower-corner launchers. | Implemented. | Replaced the underused lower-left Shift with `KC_MSEL` for Media Player and the lower-right Shift with `KC_CALC` for Calculator on both Base and Colemak. This is only a Base/Colemak typing-layer launcher change; it does not change the A-family Media layer and does not include Colemak toggle placement. Needs compile and physical trial. |
| 2026-05-08 | Discuss Navigation double-tap window snapping. | Discussed. | Requirement: on the Navigation layer only, single-tap `J/I/K/L` remains Left/Up/Down/Right, while double-tap `J/I/K/L` sends Windows snap shortcuts `GUI+Left/Up/Down/Right`. This deliberately avoids adding a hidden GUI modifier key to the Navigation layer, because that would disturb the home-row modifier order. Risk to research before implementation: repeated arrows may accidentally trigger window snapping, so the double-tap timing must be tight and the feature must be easy to roll back. |
| 2026-05-08 | Trial Media Y/H refinement. | Follow-up needed. | User reported Play/Pause and Mute work on `A + left GUI`, confirming the layer and consumer media keys are active. Previous/Next on `J/L` did not appear to work in the tested app. Next check: test `KC_MPRV`/`KC_MNXT` in a media app known to support track navigation, such as Spotify or VLC, and verify Volume Up/Down on `I/K`. |
| 2026-05-12 | Capture next requirement batch. | Requirement added. | Added candidate requirements for moving Function keys to the A-family System layer, redesigning Symbols toward top-row symbols with transparent/blank remaining positions, researching Filterpaper's Colemak RGB behavior, researching community/QMK home-row-mod anti-accidental-typing settings, and exploring red RGB error feedback for blank command-layer keys with a different Caps indicator approach. |
| 2026-05-12 | Research accidental home-row activation. | Researched. | Current config uses `TAPPING_TERM 220`, `FLOW_TAP_TERM 150`, `QUICK_TAP_TERM 110`, per-key permissive hold, per-key hold-on-other-key-press, and inherited Filterpaper `IS_TYPING()` callbacks. QMK now documents Flow Tap as a recommended 150ms starting point for home-row mods and Chordal Hold as an opposite-hands rule to prevent same-hand rolling accidents. Recommended next experiment: enable `CHORDAL_HOLD`, define handedness for the Corne matrix if needed, compile, and trial ordinary typing before changing tapping terms again. Include the concrete `?` then `f/` sequence in the physical typing trial because it is a repeated real-world follow-through pattern. |
| 2026-05-12 | Capture Numbers bracket refinement. | Requirement added. | Requirement idea: on the Numbers layer, the current left/right parenthesis keys remain round brackets on tap, become square brackets on hold, and become curly brackets on double tap. This should be researched before implementation because it introduces Tap Dance or similar timing behavior inside an already-held layer. |
| 2026-05-12 | Implement Chordal Hold, top-row Symbols, and punctuation tap dances. | Kept. | Enabled `CHORDAL_HOLD`; moved Symbols to a top-row-owned surface with transparent lower rows; added tap/hold/double-tap behavior for Symbols brackets and Base/Colemak slash/quote keys; kept A-family thumb refinements for Media, Mouse, and System. Physical testing confirmed the new Symbols layout, transparent lower rows, bracket tap dances, Base/Colemak punctuation tap dances, and A-family thumbs work. |
| 2026-05-12 | Refine Selection-layer retirement requirement. | Discussed. | Selection behavior can be composed from plain Shift/Ctrl plus arrows and extremes, so the dedicated Selection layer should be removed. Candidate redesign: S/L + GUI remains arrows, S/L + Alt becomes extremes, and S/L + Space becomes `SNP`, a snap-only GUI+Arrows/window layer. `SNP` should use the same `I/J/K/L` shape, keep Delete on Backspace, avoid Ctrl/Shift positions, and light only `I/J/K/L` plus Delete in RGB. |
| 2026-05-12 | Refine Function/System RGB requirement. | Requirement added. | When Function keys move to `A + Space`, their momentary command-surface RGB should use a different color from Colemak. Colemak remains a persistent whole-board typing-mode color; Function/System should light only usable command keys in a distinct command-layer color. |
| 2026-05-12 | Capture firmware-size reduction candidates. | Requirement added. | Firmware is near the size ceiling, so future feature work should include measured size tradeoffs. Highest-priority investigation is Mouse Keys cost, followed by minimal OLED, custom tap/hold/double handling, lighter RGB indicators, and layer cleanup. |
| 2026-05-12 | Implement A-family Function/System layer. | Kept. | Removed function keys from the left side of Numbers/Commands and moved them to `A + Space` on the right hand in the number-pad shape. Colemak toggle is now on the quote position in that layer. Function/System uses its own command-layer RGB color distinct from persistent Colemak purple. Physical testing confirmed the Function/System layout, Colemak toggle, and Numbers regression checks work. |
| 2026-05-12 | Implement Selection removal and SNP snap layer. | Implemented. | Removed `_SELE` from the active Corne recipe, moved Extremes to `S/L + left Alt`, and added `SNP` on `S/L + left Space` with `I/J/K/L = GUI+Up/Left/Down/Right`. RGB now treats `SNP` as a command layer that lights only the directional shape plus Delete. Needs compile, flash, and physical trial. |
| 2026-05-12 | Research and trial blank-key RGB feedback. | Compiled. | QMK documents both host-state indicators such as Caps Lock and layer indicators based on configured keycodes. Community Corne/split feedback is mixed: many users rely on momentary-layer feel, some use one color per layer or highlighted useful keys, and some prefer printed/engraved layer legends. No strong community pattern was found for red "blank key" feedback, so this is an explicit ChieftainDots learning-mode experiment. Implemented flat red for intentionally blank command-layer keys and corrected Numbers RGB so the retired left-side Function area no longer lights as useful. Compile succeeded at 28544/28672 bytes, with 128 bytes free. A pulsing Caps/Colemak variant failed size at 28730/28672 bytes, 58 bytes over. |
| 2026-05-12 | Re-establish working RGB baseline after animated-state experiment. | Working baseline restored; RGB follow-up pending. | Physical testing showed the right-half dropout was caused by a cable connection issue, not firmware. The diagnostic firmware with direct Caps/Colemak state painting restored reliable typing and visible red Caps feedback. Core keyboard behavior appears good again after testing, but RGB behavior still needs a more deliberate pass layer-by-layer before the indicator model is considered fully settled. |
| 2026-05-12 | Replace Mouse with Text Snippets. | Compiled. | Disabled `MOUSEKEY_ENABLE`, replaced `_MOUS` with `_TEXT`, and mapped `A/; + left Alt` to six snippets: `Y = Home`, `H = Work`, `I = Phone`, `J = Meeting`, `K = Email`, and `L = Name`. Real snippet values live in ignored local `features/text_stubs_private.h`; committed files contain placeholders/examples only. The QMK wrapper timed out at check-size after generating the hex, but parsing the generated hex shows 28182/28672 bytes, about 490 bytes free. Needs normal compile confirmation if desired, then flash and physical trial. |
| 2026-05-12 | Refine RGB feedback after text-stub trial. | Compiled. | Removed always-red blank-key maps. Blank command-layer key presses now trigger a short whole-board red flash. Colemak now uses Filterpaper-style Candy Rain. The first standalone red Candy Caps implementation exceeded flash size at 28766/28672 bytes. Refactoring Candy Rain into a shared parameterized engine let Colemak keep rainbow Candy and Caps use a red-only Candy variant while fitting at 28606/28672 bytes, with 66 bytes free. |
| 2026-05-13 | Pause large generated OLED artwork. | Requirement added. | Generated 30x16 outline/filled artwork would require roughly 1KB or more, which is too expensive for the current firmware budget. Reverted OLED experiments to the committed Filterpaper-style baseline. Future OLED work should start with compact artwork for the four home-row concepts only, then separately evaluate whether unused-helper cleanup or removing Bongocat frees enough memory for a full chord-discovery OLED concept. |
| 2026-05-13 | Trial minimal Filterpaper-style OLED reuse. | Rolled Back. | Tried to keep Bongocat untouched and use existing Filterpaper OLED glyphs for four home-row concepts. Compile succeeded after removing unused legacy helper functions and reduced firmware to 27984/28672 bytes, but physical test showed two old layer-status images plus two partly drawn modifier fragments. Lesson: the old Symbols/Numbers layer icons can be reused, but the Ctrl/Shift modifier glyphs are not standalone vertical tiles. Reverted to Filterpaper's original committed `oled-icons.c`. |
| 2026-05-15 | Make "byte is king for unused code" explicit. | Requirement added. | The large OLED experiment showed that even small-looking artwork can be too expensive near the ATmega32U4 limit. Future cleanup should ask what active behavior uses each compiled feature, helper, source file, and QMK option. Unused code should be disabled or removed, measured by compile, and kept in git history rather than firmware. |
| 2026-05-14 | Capture Numbers operator-memory concern. | Requirement added. | Multiplication, full stop, and division may conflict with traditional keyboard muscle memory; older top-row number memory may be a useful design resource. Do not change the working number layer casually; first compare operator placement options against real mistype frequency, calculator ergonomics, and transfer back to a traditional keyboard. |
| 2026-05-14 | Lock candidate Numbers symbol-shift trial. | Requirement added. | Candidate trial: Numbers top row follows traditional number-row memory across both halves, right-hand lower rows keep a compact numpad core, and the normal left-hand Shift position becomes a momentary Symbols key while Numbers is held. The symbol-shifted surface should show shifted-number symbols in the same grid; the symbol-shift layer itself does not need a `SYM` key. Known compromise: `=` and `+` cannot both be perfectly traditional on Corne, so their placement must be judged by Excel/calculator usefulness and mistype rate. |
| 2026-05-14 | Implement Numbers symbol-shift and eager Shift trial. | Compiled. | Numbers now combines top-row number memory with a compact right-hand pad, and the `F` position enters the overridden Symbols surface while Numbers is held. Symbols is now a shifted-number trial surface rather than a transparent letter passthrough layer. `F` and `J` home-row Shift bypass Flow Tap and are the only eager hold-on-other-key-press keys; Numbers/Symbols layer taps stay deliberate. Test `?`, capital `I`, ordinary `fi` words, and accidental number activation. Compile succeeded at 28276/28672 bytes, leaving 396 bytes free. |
| 2026-05-14 | Implement Symbols edge-key refinement. | Implemented. | The overridden Numbers/Symbols row trial now follows the unshifted/shifted keyboard-row model: Numbers gets `` ` `` and Symbols gets `~`. Quote-key tap dance for `` ` `` and `~` was removed, so quote tap remains quote and quote hold/double tap do nothing special. Also reconsider whether Delete belongs on Numbers/Symbols now that these layers are character-entry surfaces rather than editing/navigation surfaces. |
| 2026-05-15 | Audit unused compiled helpers. | Compiled. | Reviewed current roadmap/history before deleting helpers. The legacy `TH_*` clipboard/tap-hold path came from Filterpaper-era combo behavior, while current ChieftainDots has combos disabled after ergonomic trial feedback. Removing the active compiled handler and keycode aliases saved 126 bytes: firmware moved from 28396/28672 to 28270/28672 bytes. Remaining combo files, archived code, and layout converters are mostly inactive or preprocessor-only and should be treated as readability cleanup rather than byte wins unless they are included in an active build. |
| 2026-05-15 | Remove RGB Matrix keypress tracking. | Kept. | Disabled `RGB_MATRIX_KEYPRESSES` because current ChieftainDots RGB indicators are layer/state driven and no longer use QMK reactive keypress animations. Compile succeeded at 27792/28672 bytes, leaving 880 bytes free, a 372-byte saving from the previous 28164-byte build. User flashed it and reported things are looking good. |
| 2026-05-15 | Capture possible loss from removing RGB Matrix keypress tracking. | Requirement added. | What we lose is not current ChieftainDots behavior, but the option to use QMK reactive keypress effects without paying the bytes again. QMK's RGB Matrix keypress tracking is useful for effects that respond to a specific pressed key. If we restore it later, prefer indicator-style uses that fit the visual language: command confirmation, snippet confirmation, or error feedback. Treat the budget as roughly 372 bytes before custom effect code. |
| 2026-05-15 | Roadmap and architecture cleanup. | Implemented. | Added explicit maintainability priorities for roadmap cleanup, architecture ownership, legacy helper audit, source-of-truth cleanup, and status hygiene. Updated the architecture document so it reflects the current active layer model, RGB vocabulary, and file ownership boundaries. No firmware behavior changed. |
| 2026-05-15 | Legacy helper audit for mental clarity. | Implemented. | `keymaps/corne.json` uses `LAYOUT_crkbd_w` and the current `_BASE/_COLE/_NUMB/_SYMB/_NAV/_EXTR/_SNP/_MEDI/_TEXT/_SYST` layer set. Removed stale Filterpaper-inherited `keymaps/cradio.json` and `keymaps/technik.json`, plus their unused wrappers, because they referenced removed `_FUNC` and gave a false signal of supported multi-keyboard builds. Architecture docs now describe how to create fresh recipes for slightly smaller or larger keyboards from the current ChieftainDots model. |
| 2026-05-19 | Remove stale Cradio/Technik build hooks. | Kept. | Audited the remaining `rules.mk` references. They were not layer recipes; they were inherited per-keyboard feature switches for Cradio autocorrect/debounce/KB2040 RGB and Technik O RGB. Because ChieftainDots no longer has active Cradio or Technik recipes or wrappers, those hooks were removed from active build rules. Future board support should reintroduce board-specific rules only alongside a fresh current-model recipe and compile test. |
| 2026-05-19 | Discuss Spanish accented vowel access. | Discussed. | Paused the `=`/`+` refinement because `+` is already available from Symbols and should be lived with for a while. Next language feature is Spanish accented vowels. Initial research: QMK language extras provide Spanish keycode/sendstring headers but require matching host layout; `SEND_STRING()` cannot send accented Unicode directly; QMK Unicode can send code points but needs host setup, with WinCompose recommended on Windows. Decide whether the first trial should be host-layout dead keys, WinCompose Unicode, or a custom accent helper before implementation. |
| 2026-05-20 | Keep Spanish lower-corner access. | Kept. | Considered moving Spanish access to hold Caps and hold quote so Media Player and Calculator could return to the lower corners. Decision: keep the committed lower-corner Spanish layer because it already works physically, avoids adding new hold-tap timing risk to Caps/quote, and avoids the Colemak conflict where right-home `;` would both hold Spanish and be needed for `ü`. Revisit only if the lower-corner reach becomes a real typing problem. |
| 2026-06-02 | Capture simplification requirements and keep auto-caps production path. | Compiled. | Added roadmap requirements for the two-family simplified layer model, dismissed alternatives, MS Styles, and future logging/doc follow-through. Committed the Tab double-tap guard so Alt/GUI traversal does not close windows. Refactored auto-caps so behavior no longer depends on `CONSOLE_ENABLE`; console logging remains dormant, Spanish opening punctuation can prime capitalization, and Spanish compose handles weak Shift for uppercase accented starts. OLED-on production build was 28908/28672 bytes, 236 bytes over, so the kept compiled path has console off and OLED disabled. Compile succeeded at 21986/28672 bytes, leaving 6686 bytes free. |
| 2026-05-15 | Preserve old layout-wrapper reference. | Implemented. | Added lightweight git tag `filterpaper-layout-wrappers-20260515` at the last committed version before deleting the stale Filterpaper conversion helpers. Use `git show filterpaper-layout-wrappers-20260515:layout.h` to inspect the old code later. This keeps the Mountain Ergo-style wrapper pattern retrievable without keeping stale helpers in active source. |
| 2026-05-15 | Start live `F/J` typing-timing trial. | Flashed. | Disabled hold-on-other-key-press for `F`/`J` Shift mod-taps after accidental capital `O` appeared when intending `f` then `o`. The trial keeps permissive hold, Flow Tap, and Chordal Hold. User flashed and reported things look good for now, but this should be judged during ordinary typing rather than staged test phrases only. |
| 2026-05-15 | Roadmap status hygiene pass. | Implemented. | Promoted settled items out of temporary states: legacy layout alias cleanup, RGB indicator-first behavior, A-family System, and the status-hygiene task itself. Marked the `F/J` timing change as a live flashed trial rather than `Kept` because it needs real typing time. |

## Feature Template

Use this shape when promoting an item from Idea:

```text
Feature:
Status:
Intent:
Principle supported or challenged:
Architecture impact:
QMK compatibility notes:
Firmware size risk:
Acceptance criteria:
Rollback plan:
```
